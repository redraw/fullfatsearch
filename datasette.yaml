databases:
  youtube:
    # allow:
    #   id: root
    tables:
      subtitles_with_videos:
        fts_table: subtitles_fts
        fts_pk: subtitle_id
        searchmode: raw
plugins:
  datasette-auth-passwords:
    root_password_hash:
      $env: ROOT_PASSWORD_HASH
